# SQL Reset Scaffold

The active SQL schema for the reset-era rebuild has not been frozen yet.

This directory remains in place for the next round of work, when the base
Supabase storage model is defined from the smaller lineage contract.

The previous staged bootstrap SQL has been archived under `legacy/sql/`.
