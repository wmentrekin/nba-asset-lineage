# CLAUDE.md

See `AGENTS.md` for full repo context and conventions.

This repo also imports a shared agent workflow at `.agents/` (git subtree). Read
`.agents/AGENTS.md` before starting substantial work; invoke it with `/work`.
