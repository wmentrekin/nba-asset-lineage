artifact: status
feature: "foundation/data-gap-closure"
mode: "bounded-feature"
stage: "verification"
owner_skill: work

objective: "Close the remaining `missing_required_evidence = 38` draft/pick corroboration gap in one bulk pass."

summary:
  current_focus: "Draft/pick `missing_required_evidence` is closed; the next decision is how to attack the larger `recognized_provider_not_loaded` bucket."
  last_completed: "Loaded one generated `curated_fixture` draft-pick-detail source record plus 20 corroboration-only draft events, fixed draft-specific audit reconciliation, and reduced `missing_required_evidence` from 38 to 0 without changing the canonical graph."
  current_blocker: ""

artifacts:
  requirements: "docs/foundation/data-gap-closure/requirements.yaml"
  plan: "docs/foundation/data-gap-closure/plan.yaml"
  implementation_report: "docs/foundation/data-gap-closure/implementation-report.yaml"

agents:
  - id: "orchestrator"
    role: "orchestrator"
    model_guidance: "xhigh"
    status: "active"
    stage: "verification"
    task: "Record the final live verification state and hand off the next remaining gap."
    last_update: "Focused tests, full pytest, live preview/dry-run, live execute, live audit, and live graph-baseline verification all completed."
    blocker: ""
  - id: "developer-draft-loader"
    role: "developer"
    model_guidance: "high"
    status: "completed"
    stage: "execution"
    task: "Implement the curated draft-pick-detail preview/load path and focused loader tests."
    last_update: "Completed deterministic generation from loaded draft_selection rows plus new preview/load CLI commands and focused loader tests."
    blocker: ""
  - id: "developer-draft-audit"
    role: "developer"
    model_guidance: "high"
    status: "completed"
    stage: "execution"
    task: "Implement draft-specific audit reconciliation and focused audit tests."
    last_update: "Completed exact draft reconciliation keyed by draft_selection_id, duplicate-draft reuse, the GG/Gregory alias exception, and the 2025 nearby-day duplicate fix."
    blocker: ""

loops:
  planning_review:
    count: 1
    max: 2
  execution_verification:
    count: 1
    max: 2

checkpoint:
  pre_execution_required: true
  pre_execution_status: approved
  options:
    - proceed
    - revise_plan
    - narrow_scope
    - stop

blockers: []
decisions:
  - "Treat the `bref_only` closeout as complete and move the active gap-closure slice to `missing_required_evidence`."
  - "Define this slice as a one-pass closeout of the 38 remaining draft/pick rows, not another partial residue-reduction pass."
  - "Use the existing `corroboration_only` path and keep canonical graph invariance guarantees."
  - "Assume the smallest truthful finish line is `missing_required_evidence -> 0`, even if many touched events land only as `recognized_provider_not_loaded` afterward."
  - "Use one generated `curated_fixture` draft-pick-detail source record derived from loaded Memphis draft_selection truth rather than introducing a new SQL surface."
  - "Keep draft matching exact on player plus `draft_selection_id`, then allow narrow duplicate-row reuse for same-day and 2025 second-round nearby-day draft rows."

next_action: "Choose the next remaining gap to attack, with `recognized_provider_not_loaded` now the largest live bucket."
next_recommended_command: "$work"
user_input_needed: false
