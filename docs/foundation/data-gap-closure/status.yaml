artifact: status
feature: "foundation/data-gap-closure"
mode: "large-initiative"
stage: "completed"
owner_skill: work

objective: "Close the remaining `recognized_provider_not_loaded = 368` live gap in one bounded pass without weakening the minimum truthful graph contract."

summary:
  current_focus: "Completed the recognized-provider closeout and replaced that bucket with explicit supplemental-gap tracking."
  last_completed: "Closed `recognized_provider_not_loaded` to zero while preserving canonical graph invariance."
  current_blocker: ""

artifacts:
  requirements: "docs/foundation/data-gap-closure/requirements.yaml"
  plan: "docs/foundation/data-gap-closure/plan.yaml"
  implementation_report: "docs/foundation/data-gap-closure/implementation-report.yaml"

agents:
  - id: "orchestrator"
    role: "orchestrator"
    model_guidance: "xhigh"
    status: "completed"
    stage: "verification"
    task: "Executed the recognized-provider closeout, verified the live audit delta, and recorded the resulting supplemental-gap metrics."
    last_update: "Live audit now reports `recognized_provider_not_loaded=0`, `meets_minimum=404`, `out_of_scope=3`, and `events_with_missing_supplemental_roles=368`."
    blocker: ""
  - id: "019e4ba8-9bc9-72e0-b41d-e01c971db583"
    role: "repo-researcher"
    model_guidance: "high"
    status: "completed"
    stage: "discovery"
    task: "Assess the `basketball_reference|nba_player_movement` surface."
    last_update: "Recommended treating the 261-event NBA-movement residue as policy/contract residue, not a new source-loading problem."
    blocker: ""
  - id: "019e4ba8-9c7a-7423-827d-6519a44e3edf"
    role: "repo-researcher"
    model_guidance: "high"
    status: "completed"
    stage: "discovery"
    task: "Assess the `basketball_reference|team_official` surface."
    last_update: "Warned against a blind shortcut, but the live audit shows the 69-event team-official residue already reconciles cleanly to loaded official rows and is being held open by the current minimum-versus-supplemental semantics."
    blocker: ""
  - id: "019e4ba8-9d36-7063-b67c-882037f020a0"
    role: "repo-researcher"
    model_guidance: "high"
    status: "completed"
    stage: "discovery"
    task: "Assess the `basketball_reference|curated_fixture` draft surface."
    last_update: "Confirmed the 38 draft rows already satisfy the required `secondary_pick_detail` role and are open only because supplemental official confirmation is still being counted as required for `meets_minimum`."
    blocker: ""

loops:
  planning_review:
    count: 2
    max: 2
  execution_verification:
    count: 2
    max: 2

checkpoint:
  pre_execution_required: true
  pre_execution_status: approved_and_completed
  options:
    - proceed
    - revise_plan
    - narrow_scope
    - stop

blockers: []
decisions:
  - "Treat the `368`-event residue as a corroboration-contract gap first, not as a new data-ingestion gap."
  - "Keep the existing `planned_vs_loaded_rule`; no provider role counts unless a matching loaded row exists."
  - "Use the live split of `261` `basketball_reference|nba_player_movement`, `69` `basketball_reference|team_official`, and `38` `basketball_reference|curated_fixture` as the execution target surface."
  - "Do not weaken the contract enough to let `basketball_reference`-only events meet minimum."
  - "Track missing official/structured depth through explicit supplemental-gap metrics instead of the old `recognized_provider_not_loaded` status bucket."

next_action: "Choose the next structural data gap now that the corroboration-contract gap is closed."
next_recommended_command: "$work"
user_input_needed: false
