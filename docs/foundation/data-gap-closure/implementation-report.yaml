artifact: implementation_report
feature: "foundation/data-gap-closure"
mode: "bounded-feature"
status: completed
owner_skill: work
based_on: "docs/foundation/data-gap-closure/plan.yaml"

summary:
  scope_executed: "Executed the one-pass `recognized_provider_not_loaded` closeout by refining the source-policy/audit contract so minimum truthful corroboration is separate from supplemental corroboration depth. No database write was required."
  outcome: "The live `recognized_provider_not_loaded` bucket fell from 368 to 0. Post-closeout live state is `meets_minimum=404`, `out_of_scope=3`, `bref_only=0`, and `missing_required_evidence=0`. Supplemental corroboration depth remains measurable through `events_with_missing_supplemental_roles=368` and role-level counts `official_confirmation=299`, `structured_player_movement=69`. Canonical graph truth remained unchanged at 407 canonical events, 420 canonical members, 568 transitions, and checksum `a9748140971aa69146311c9cd9d2b9ee7ec9d4f1319a0d8262d94d66390a3f8b`."

task_batches_attempted:
  - "B1"
  - "B2"
  - "B3"
task_batches_completed:
  - "B1 minimum-versus-supplemental corroboration contract update"
  - "B2 audit output and summary enrichment for supplemental gaps"
  - "B3 local and live verification"

changes:
  files:
    - "docs/foundation/data-gap-closure/status.yaml"
    - "docs/foundation/data-gap-closure/implementation-report.yaml"
    - "docs/foundation/full-span-load-notes.md"
  modules:
    - "src/foundation/sources.py"
    - "src/foundation/audit.py"
    - "tests/foundation/test_audit.py"
    - "tests/foundation/test_sql_contracts.py"
  docs_updated:
    - "Recorded the new source policy version and the separation between minimum truth and supplemental corroboration depth."
    - "Recorded the final live audit delta and the new supplemental-gap summary metrics."

local_validation:
  checks_run:
    - "./.venv/bin/pytest tests/foundation/test_audit.py"
    - "./.venv/bin/pytest"
  failures: []
  skipped: []

live_validation:
  checks_run:
    - "./.venv/bin/python -m redesign_cli audit-foundation-data"
    - "./.venv/bin/python -m redesign_cli inspect-foundation-graph-baseline"
  results:
    source_corroboration:
      before:
        recognized_provider_not_loaded: 368
        meets_minimum: 36
        bref_only: 0
        missing_required_evidence: 0
      after:
        recognized_provider_not_loaded: 0
        meets_minimum: 404
        out_of_scope: 3
        bref_only: 0
        missing_required_evidence: 0
        events_with_missing_supplemental_roles: 368
        by_missing_supplemental_role:
          official_confirmation: 299
          structured_player_movement: 69
    graph_baseline:
      canonical_event: 407
      canonical_event_member: 420
      event_asset_transition: 568
      graph_export_checksum_sha256: "a9748140971aa69146311c9cd9d2b9ee7ec9d4f1319a0d8262d94d66390a3f8b"
  failures: []

review_summary:
  status: manually_verified
  key_findings:
    - "The old `recognized_provider_not_loaded` bucket was primarily semantic residue, not a missing-ingest surface."
    - "The live team-official residue already reconciled cleanly to loaded official rows; it now counts as minimum truth while still exposing the missing structured movement depth."
    - "The live NBA player movement residue now counts as minimum truth while still exposing missing official confirmation depth."
    - "The draft `curated_fixture` residue now counts as minimum truth while still exposing missing official confirmation depth."
    - "No basketball-reference-only event was promoted by the new contract."

test_summary:
  status: full_passed
  checks_run:
    - "tests/foundation/test_audit.py"
    - "tests/foundation/test_sql_contracts.py"
    - "full pytest"
    - "live read-only audit"
    - "live graph baseline inspection"
  failures: []
  gaps: []

open_issues:
  - "Supplemental corroboration depth is still incomplete for 368 events, but it is no longer blocking the minimum truthful graph contract."
  - "Official roster checkpoints, daily roster state, two-way completeness, contract-semantics depth, NBA stats identity loading, future-pick replay, conditional pick branches, and prior-owner draft lineage remain structural gaps."

handoff:
  current_state: completed
  next_owner: "orchestrator"
  next_action: "Choose the next structural gap after the corroboration contract closeout, with supplemental-depth tracking now available through the audit summary instead of the old recognized-provider bucket."
