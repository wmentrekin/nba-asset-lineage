artifact: task_handoff
feature: "foundation/safe-refresh-tooling"
mode: "large-initiative"
execution_batch_id: "B4"
task_id: "T11"
assigned_domain: "data-engineer"
branch: "work/safe-refresh-tooling"
owner_skill: work
objective: "Document capture, projection, approval, runner, and restore safety contracts; record sensitivity, retention, limitations, and the parent-refresh handoff; run the full local validation ladder."
why_this_matters: "Future operators must distinguish read-only preview, execution approval, and destructive restore, and reproduce validation without attempting live operations."
inputs:
  requirements: "docs/foundation/safe-refresh-tooling/requirements.yaml"
  plan: "docs/foundation/safe-refresh-tooling/plan.yaml"
  implementation_report: "docs/foundation/safe-refresh-tooling/implementation-report.yaml"
  relevant_paths: ["README.md", "docs/foundation/README.md", "docs/foundation/full-span-load-notes.md", "docs/foundation/safe-refresh-tooling/", "mise.toml", "src/redesign_cli.py", "src/foundation/refresh_safety.py", "src/foundation/source_payloads.py", "src/foundation/refresh_projection.py"]
scope:
  owned: ["README.md", "docs/foundation/README.md", "docs/foundation/full-span-load-notes.md", "docs/foundation/safe-refresh-tooling/", "mise.toml"]
  forbidden: ["Actual source capture", "Actual database snapshot/run/restore", "Offseason data changes", "personal-site/", "SQL migrations", "Runtime source changes outside documentation/mise scope"]
implementation_notes:
  - "Document commands and safety boundaries truthfully; do not claim a concrete live run command exists where T9 deliberately exposes only a typed execution seam."
  - "Clearly label restricted local operational artifacts, retention/cleanup, and the separate approval required for restore."
  - "Preserve the parent offseason-refresh block until this PR’s review/verification completes; document the parent handoff."
  - "Run only local test/help/build checks. Record missing-tool/dependency limitations rather than installing or using live services."
success_criteria: ["Operator docs distinguish capture/preview/approval/run/restore", "Sensitivity/retention/limitations are explicit", "Parent refresh handoff is documented", "Focused/full Python, frontend checks/build, and CLI help are attempted and recorded", "No live systems touched"]
validation_expected: ["Focused new tests", "Full Python suite", "Frontend tests/check/build", "CLI --help smoke checks"]
output_format:
  sections: [Changes, Local Validation, Blockers, Notes]
escalate_if: ["Documentation needs an unsupported product claim", "A required validation needs live access", "The full suite fails outside owned documentation scope"]
